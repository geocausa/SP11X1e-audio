# UbiG M6 machine-verifiable closure bundle — 2026-08-23

This bundle records the remaining machine-verifiable M6 gates on the active v4
source-owned candidate. It does not claim the final matched Windows physical
acoustic verdict.

- continuous activation exceeded eight hours with PID 599944 and zero restarts;
- the hash-matched retained Seven Nation Army program completed the three
  deterministic FLUSH|ACCURATE seeks under exact endpoint DSP mute, with the
  UbiG control page and services unchanged;
- a separate 30-second **unmuted physical-output** run at the existing 14%
  visible volume retained PA=1, status 2f/00, errors 00/00, current-limit code
  17/no override, no failed WSA reads and no kernel/filter faults on both amps;
- the tracked Stage-B v4 private-pack builder regenerated the active private
  pack byte-for-byte from owner-supplied private inputs. No private payload is
  stored in Git.

The only remaining M6 promotion gate is the matched Windows-vs-UbiG physical
acoustic matrix / operator listening verdict. Golden rollback remains installed.
