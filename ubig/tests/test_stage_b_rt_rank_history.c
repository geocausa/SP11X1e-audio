#include "stage_b_rt.h"
#include <stdint.h>
#include <stdio.h>
static uint32_t rng=0x472db681u;static uint32_t ru(void){rng=rng*1664525u+1013904223u;return rng;}static float fr(float a,float b){return a+(b-a)*(float)(ru()>>8)*(1.0f/16777216.0f);}static uint64_t h64(uint64_t h,const void*p,size_t n){const unsigned char*b=p;for(size_t i=0;i<n;i++){h^=b[i];h*=1099511628211ULL;}return h;}
int main(void){UbigStageBRtRankHistory s={0};float out[10]={0},scratch[64]={0};s.cursor.step=5;s.cursor.index=9;uint64_t h=1469598103934665603ULL;for(unsigned t=0;t<50000;t++){for(unsigned r=0;r<32;r++)for(unsigned c=0;c<3;c++)s.matrix[r][c]=(ru()%19u==0u)?0.0f:fr(0,3);s.cursor.step=ru()%32u;float control=(ru()%23u==0u)?0.0f:fr(0.0001f,3);ubig_stage_b_rt_rank_history_process(&s,control,out,scratch);h=h64(h,out,sizeof out);h=h64(h,scratch,sizeof scratch);h=h64(h,&s.cursor,sizeof s.cursor);}if(h!=0x2176092f17bc1f64ULL){fprintf(stderr,"Stage-B rank-history hash %016llx\n",(unsigned long long)h);return 2;}puts("PASS Stage-B RT rank-history regression");return 0;}
