# SP11 Windows volume transaction candidate — 2026-08-13

## Status

Built, signed, installed as an isolated GRUB entry, and ready for one-shot live
validation. This is a diagnostic candidate, not a promoted fix. Persistent
GRUB default remains `sp11-audio-cps-v3`.

## Exact scope

The candidate changes only `snd-q6apm` relative to the accepted CPS-v3
initramfs. It adds fixed-target ALSA bytes control
`SP11 Windows Volume Transaction`. The kernel validates a userspace payload as:

- one Q28 gain at or below unity;
- exactly four ordered iid `0x489e` frames;
- pids `0x08001020`, `0x08001021`, `0x08001022`, `0x08001026`;
- exact 216-byte or 272-byte total size and zero alignment padding.

On a running protected graph it sends the exact 104-byte final
`0x4a63/0x08001038` volume body first, followed by the complete GainStep delta
through the existing OOB graph-calibration mapping. Arbitrary module targets,
malformed payloads and idle graphs are rejected.

Userspace ordering is Dolby postgain, combined DSP transaction, then hardware
sink unity. While idle, on failure and on service exit it establishes/restores
host attenuation. The dispatcher detects the candidate ALSA control; rollback
kernels automatically exec the established legacy volume actuator, and the
separate legacy MSIIR service stands down only when the combined control is
present.

## Provenance

- accepted kernel source base: `11d875d`;
- candidate kernel commit: `a934531`;
- canonical patch SHA-256: `53c4a0d8d0846d7f666692825b1985264b5357c282007229d7282c8a7bbee490`;
- unsigned `snd-q6apm.ko`: `887629ef1b6d251ec0467eccfa29380aca06771498131ede1c2d9406768a8115`;
- signed `snd-q6apm.ko`: `0b951123128377f2e30b6a7e518fe5bb9e0bded09053351c53ab3785051530bf`;
- initramfs `snd-q6apm.ko.zst`: `390445ecdf9dc6e57eeec1b88c6945e774ae8ddb206aeedf8760b8f5ef700b8e`;
- candidate initramfs: `9086202d40521bfc13cf83cc23550a47852e8f2fdbf2a95a3961a7512f499afa`;
- reused accepted kernel: `164bc92d88c724ac4e7872212405c8149cd52a8ee1553d54e11d56581751fc48`;
- reused accepted DTB: `7cd5fdd8ef59c46ca9a3661adacce0444893a6c26fca71c97eaa3070a88aab84`;
- module srcversion: `7807B09DF71E80C7743045B`;
- vermagic: `7.1.5-sp11-cps-v3+ SMP preempt mod_unload modversions aarch64`;
- signer: `Build time autogenerated kernel key`;
- signing key: `5A:58:B6:3A:CA:17:D4:5F:02:D0:58:3E:39:7E:39:87:55:B5:62:47`.

The initramfs comparison found only `snd-q6apm.ko.zst` changed among actual
modules. `modules.symbols` and its binary index were regenerated because the
candidate exports `audioreach_sp11_set_final_volume_q28`. The accepted DTB
model remains `X1E80100-Microsoft-Surface-Pro-11-CPS-Parity-V2`. The Wi-Fi
kernel-bake verifier accepted both the source hook and compiled DTB property.

## Verification completed before reboot

- strict kernel checkpatch: 0 errors, 0 warnings, 0 checks;
- qdsp6 module build with `W=1`: pass;
- source/object/module provenance verifier: pass;
- Wi-Fi RF-kill survival verifier: pass;
- GRUB config syntax: pass;
- pytest: 133 passed, 3 private-capture skips, 6 subtests passed;
- unittest: 104 passed, 3 private-capture skips;
- current rollback boot: Wi-Fi connected, audio services active, dispatcher
  correctly selected the legacy actuator because the candidate control was
  absent.

## Installed paths

- boot bundle: `/boot/sp11-7.1.5-audio-volume-transaction/`;
- GRUB generator: `/etc/grub.d/59_sp11_audio_volume_transaction`;
- candidate module staging:
  `/usr/local/lib/sp11-audio-candidates/volume-transaction/snd-q6apm.ko.zst`;
- one-shot GRUB id: `sp11-audio-volume-transaction`;
- persistent fallback GRUB id: `sp11-audio-cps-v3`.

## Post-reboot gate

Confirm `sp11_entry=7.1.5-sp11-volume-transaction`, Wi-Fi connectivity, exact
loaded module signer/srcversion, presence of the combined ALSA control, and
that the combined synchronizer owns volume while the legacy MSIIR service has
exited cleanly. Begin at low volume, then validate ordinary playback, slider
changes and the known YouTube seek cases. Any boot, network or audio failure
requires no GRUB repair: rebooting again returns to persistent CPS-v3.
