# 2026-08-22 — first live UbiG native candidate smoke on SP11

The tracked source-owned UbiG LADSPA candidate has now crossed the first disposable M6 on-device gate. The active `filter-chain.service` loads `/home/geoca/.local/lib/ubig-candidate/ubig-sp11-candidate.so` (SHA-256 `1b3e3110c07bdc5b21e3c59ee75482a598c35e3acf27c2e600fbc6fa50355eb8`) with `MemoryDenyWriteExecute=yes`. `/proc/<filter-chain-pid>/maps` contains the UbiG candidate and no `sp11_dolby_windows_chain.so`, `DolbyAPOVR.dll` or `DolbyAPOvlldp150.dll` mapping. The private corrected-v3 data pack remains external and pinned at SHA-256 `c993c123f2cb3b92776754da2383217e00b5f290664571f12cfb62b9afb3a175`.

A first activation exposed one deployment-only issue before any audio stream: the installed endpoint-volume Python helpers were older than the source tree and therefore continued writing the legacy 12-byte control page. The candidate was rolled back to the exact saved Golden PipeWire fragment, the deployment wrapper was changed to stage its own reviewed volume dispatcher / transaction / MSIIR helpers and override only the candidate service `ExecStart` paths, then the candidate was activated again. This is why the candidate package must carry its matching control consumers rather than assume `~/.local/bin` has already been promoted.

On the corrected activation, the endpoint-volume transaction helper pre-created `/run/user/1000/ubig-control-v2`, queued endpoint postgain `-365`, and the first streamed block caused the native candidate to synchronize the cold profile to Movie and acknowledge that independent postgain generation. A 480,000-frame finite 48-kHz stereo silence stream then exercised live `Movie -> Game -> Custom(non-flat) -> Movie` control changes; main request/ack reached `3/3`, postgain stayed on its independent `1/1` generation, and the filter-chain PID did not change. Additional 144,000- and 96,000-frame streams exercised mute/unmute and idle/wake; the same filter-chain PID survived and the visible volume returned to `0.22`.

No filter-chain xrun, NaN, segfault or abort marker appeared. One kernel `APM_CMD_SET_CFG` (`0x01001006`) status-3 record occurred at protected graph start, immediately followed by the existing `graph calibration returned AR_EUNSUPPORTED; continuing as Qualcomm GSL does` message and successful acceptance of all later SP/SPVI, volume, MSIIR, mute, channel-mixer and graph-start stages. This exact status-3 graph-start record is already documented in the Golden v32 findings and is not counted as a candidate regression.

The candidate is still disposable: the Golden plugin remains installed, the exact pre-candidate PipeWire fragment is saved under `~/.local/state/ubig-candidate/`, and `deploy/ubig-candidate/switch.sh rollback` remains the recovery path. This smoke does **not** close M6. Non-silent physical output, volume sweep/seek/repeated playback, long-run stability, PA/protection telemetry parity and the Windows acoustic matrix remain required.
